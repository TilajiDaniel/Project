<Page x:Class="NutriTrackWPF.UsersWpf"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NutriTrackWPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UsersWpf">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="User Management" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
        </StackPanel>
        <WrapPanel Grid.Row="1" Margin="0,0,0,20" VerticalAlignment="Center">
            <TextBlock Text="Username:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
            <TextBox x:Name="txtUsername" Width="120" Height="28" Margin="0,0,12,0" Padding="2" VerticalContentAlignment="Center"/>

            <TextBlock Text="Email:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
            <TextBox x:Name="txtEmail" Width="150" Height="28" Margin="0,0,12,0" Padding="2" VerticalContentAlignment="Center"/>

            <TextBlock Text="Password:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
            <PasswordBox x:Name="txtPassword" Width="120" Height="28" Margin="0,0,12,0" Padding="2" VerticalContentAlignment="Center"/>

            <Button x:Name="BtnAddUser" Content="Add User" Margin="0,0,12,0" Padding="15,5" Background="#2980B9" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnAddUser_Click"/>
            <Button x:Name="BtnDeleteUser" Content="Delete" Padding="15,5" Background="#C0392B" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnDeleteUser_Click"/>
        </WrapPanel>

        <DataGrid x:Name="UsersGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  Background="White" RowBackground="White" AlternatingRowBackground="#F8F9F9"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E5E7E9"
                  BorderThickness="1" BorderBrush="#BDC3C7" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Height (cm)" Binding="{Binding HeightCm}" Width="100"/>
                <DataGridTextColumn Header="Weight (kg)" Binding="{Binding WeightKg}" Width="90"/>
                <DataGridTextColumn Header="Registration" Binding="{Binding CreatedAt, StringFormat=\{0:yyyy.MM.dd HH:mm\}}" Width="140"/>
                <DataGridTextColumn Header="Privilege" Binding="{Binding Privilege}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>